[project]
name = "qagent"
version = "0.1.0"
description = "Async vendor-questionnaire agent demonstrating production asyncio patterns"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "pydantic>=2.0"]

[tool.setuptools.packages.find]
include = ["qagent*"]
